name: Reusable Deploy
on: workflow_call #Run this wf from inside other wf
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output Information
        run: echo "Deploying & Uploading"
